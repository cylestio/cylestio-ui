/**
 * This is a placeholder test file.
 *
 * Tests are temporarily disabled during the UI revamp period.
 * This file exists only to prevent import errors in files that might reference it.
 */

describe('Dashboard', () => {
  test('placeholder test', () => {
    // This test always passes
    expect(true).toBe(true)
  })
})
